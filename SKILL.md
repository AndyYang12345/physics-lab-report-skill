---
name: physics-lab-report
description: 生成并提交大学物理实验报告。用户给出上课内容（实验名称）时进入预习模式，编写实验目的/仪器/原理/操作步骤并绘制原理图；用户给出原始数据时进入数据模式，填入实验日期与原始数据记录表。两种模式都以「座位号-姓名-实验名称.pdf」导出到提交目录。Use when the user asks to write or update a 大学物理实验报告, 预习报告, fill raw measurement data, draw 原理图, or export the lab report as a submittable PDF.
---

# 大学物理实验报告

把实验报告的**排版工作**自动化：内容由你提供，格式、插图、数据表、PDF 导出由脚本完成。

工作根目录默认 `~/物理实验报告/`，可用环境变量 `PHYSLAB_HOME` 改：

```
$PHYSLAB_HOME/
├── .profile                        个人信息（唯一来源，缺字段必须问用户）
├── templates/实验报告模板-空白.docx   空白模板（**需自备**，见下）
├── .build/<名称>/                   所有中间产物（可重生成）
│   ├── report.docx                  报告本体
│   ├── spec.json / data.json        内容规格 / 原始数据规格
│   ├── figs/*.png                   原理图
│   └── make_figs.py                 绘图脚本
└── 提交/<座位号>-<姓名>-<名称>.pdf    最终交付（**只放 PDF**）
```

> **模板要自备。** 本仓库**不附带任何学校的实验报告模板文件**（版权原因）。
> 请拿你所在学校/学院的模板，删掉里面的示例与批注文字，只留空白表单，
> 放到 `templates/实验报告模板-空白.docx`，或用 `PHYSLAB_TEMPLATE` 指向它。
> 模板结构、锚点要求见 `reference/format.md`。

**「名称」不限于实验名。** 同一门课的非实验类作业（如绪论作业）走同一套约定：
中间产物进 `.build/<名称>/`，最终 PDF 进 `提交/<座位号>-<姓名>-<名称>.pdf`。

> **提交区纪律**：`提交/` 里**只能有最终 PDF**。`.docx`、图片、preview 渲染、
> 脚本一律留在 `.build/`。交付前先 `ls 提交/` 检查一遍。

所有脚本在本 skill 的 `scripts/` 下。先跑一次环境自检：

```bash
python scripts/lab.py doctor
```

它会检查依赖、模板路径、个人信息文件与 LibreOffice，并告诉你缺什么。
（`doctor` 本身不需要任何第三方依赖，缺什么都能报出来。）

---

## 硬规则（不可违反）

1. **不得伪造实验内容与数据。** 本 Skill 只做**格式与排版自动化**：
   实验原理、操作步骤必须来自课程材料，原始数据必须来自用户本人实测。
   绝不替用户编造、补齐、"估算"任何测量值。原始数据记录表仍需指导老师签字确认，
   使用者对提交内容的真实性负全部责任。
2. **必须画原理图。** 每个实验至少 1 张，用 `scripts/figstyle.py`（matplotlib）
   自己画。**不许从网上下载图片**——版权不明、分辨率不可控、符号系统常和正文
   对不上。图里的角度和方向必须与正文公式严格自洽（算出来再画，不要示意性乱画）。
3. **个人信息只来自 `.profile`。** 先跑 `python scripts/lab.py profile` 看缺什么；
   缺失的字段**必须先问用户**，绝不猜测、绝不编造学号姓名。
4. **格式**：正文宋体、小四、1.5 倍行距；图表题注宋体五号居中。
   这是本模板的要求；若你的学校模板不同，改 `labkit.py` 顶部的格式常量。
5. **提交文件名**：`座位号-姓名-实验名称.pdf`，例如 `<座位号>-<姓名>-分光计的调整与使用.pdf`。
6. **只保留最终稿。** 预习稿和终稿同名，直接覆盖，不用分版本。
7. **实验日期在实验后才填。** 预习阶段该栏留空。
8. **实验名称有歧义必须先确认。** 例：「分光镜」和「分光计」是两个完全不同的
   实验（前者是干涉仪里的半透半反镜，后者是测角用的光谱仪）。名字错了整份报告都废。
9. **原始数据逐格核对，异常值只提示不擅改。** 先做自洽性校核（见下），
   发现可疑数据报告给用户，由用户决定，不要自己"修正"。

---

## 预习模式

触发：用户给出上课内容 / 实验名称。

**步骤**

1. `python scripts/lab.py profile` —— 确认字段齐全，缺了就问用户。
2. **确认实验名称**（有歧义必须问）；实验编号一般**不写**，用户给了才写。
3. **画原理图**：在 `.build/<实验名称>/figs/` 下写绘图脚本并运行。
   先读 `reference/figures.md`，里面有两个完整范例（结构类图、光路类图）。
4. **写 `spec.json`**（放在 `.build/<实验名称>/`，图片路径可写相对路径）：

```json
{
  "experiment": "分光计的调整与使用",
  "number": "",
  "date": "",
  "sections": [
    {"h": "实验目的："},
    {"p": "1．了解分光计的结构和测角原理，掌握分光计的调节方法与读数方法。"},
    {"h": "实验仪器："},
    {"p": "分光计（含望远镜、平行光管、载物台、读数圆盘）、三棱镜、双面平面反射镜、钠光灯。"},
    {"h": "实验原理："},
    {"p": "1．分光计的结构与测角原理"},
    {"p": "分光计由望远镜、平行光管……"},
    {"f": "figs/fig1.png", "cap": "图1  分光计结构示意图", "width_cm": 12.6},
    {"h": "实验操作步骤："},
    {"p": "一、分光计的调节"},
    {"p": "1．粗调：……"}
  ]
}
```

   - `h` = 加粗小节标题（`实验目的：`这种）
   - `p` = 正文段
   - `f` = 插图，`cap` 为题注，`width_cm` 省略则 12.6（可用宽度约 14.6cm）
   - `blank` = 空行
   - 正文里的下标（α₁、δ₀）在 spec 里直接写 Unicode 也行，但**表格里必须用
     `[["α",false],["1",true],["(左)",false]]` 这种真下标格式**（见数据模式）

5. `python scripts/lab.py build spec.json`
6. `python scripts/lab.py export "<实验名称>"`
7. `python scripts/lab.py check "<实验名称>" --figs 3`
8. **目视核对 PDF 分页**（必做，见「已知坑」）——把 PDF 转图看一遍，
   确认图和题注在同一页、没有大片异常空白。

**内容要求**（预习报告不是抄书，写到能看懂、能照着做即可）

| 小节 | 要求 |
| --- | --- |
| 实验目的 | 3 条左右，动词开头（了解/掌握/学会） |
| 实验仪器 | 列全，含规格型号（如 JJY 型分光计） |
| 实验原理 | 结构原理 + 关键公式（公式要写清各符号含义）；**必须配原理图** |
| 实验操作步骤 | 分「一、二、三」大阶段，每步可执行；调节类实验要写清自准直/各半调节等关键手法 |

---

## 数据模式

触发：用户发来原始数据（截图 / Excel / 文本表格）。

**步骤**

1. **转录数据**，逐格核对。数据只能来自用户提供的实测记录。
2. **自洽性校核**（这一步不能省，是发现读数错误的主要手段）：

   | 校核项 | 判据 |
   | --- | --- |
   | 分光计两游标读数差 | 应 ≈180°（差的绝对值偏离 180° 超过 2′ 就值得怀疑） |
   | 偏向角 δ | 应 ≈ 两游标各自算出的 \|α₁−α₂\| 的平均值 |
   | 顶角 A | 应 ≈ 180° − \|θ₁−θ₂\| |
   | 重复测量列 | 看离散度，某个点明显偏离就要报出来 |

   发现异常**报告给用户**，不要自己改数据。校核不过不是"修数据"的理由，
   而是提示用户回去核对原始记录或重测。

3. **写 `data.json`**：

```json
{
  "experiment": "分光计的调整与使用",
  "date": "9.18",
  "title": "最小偏向角测量数据",
  "header": ["K",
             [["α",false],["1",true],["(左)",false]],
             [["α",false],["1",true],["′(右)",false]],
             [["α",false],["2",true],["(左)",false]],
             [["α",false],["2",true],["′(右)",false]],
             [["δ",false],["0",true]]],
  "rows": [
    ["1", "<实测值>", "<实测值>", "<实测值>", "<实测值>", "<实测值>"]
  ],
  "widths": [800, 1440, 1440, 1440, 1440, 1440]
}
```

   表头下标必须用 `[文本, 是否下标]` 形式（→ Word 的 vertAlign），
   **不要用 Unicode 下标字符 ₁₂₀**：宋体缺这些字形，Word 里会变方框。

4. `python scripts/lab.py data data.json`
5. `python scripts/lab.py export "<实验名称>"` + `check`
6. 目视核对 PDF。

若同一个实验有多组数据（如顶角 + 偏向角），写多份 `data.json` 依次跑，
或把 `rows` 合并；表头不同就分两张表，`title` 用来说明是哪一组。

---

## 格式速查

| 位置 | 字体 | 字号 | 行距 |
| --- | --- | --- | --- |
| 封面字段值 | 宋体 | 三号(sz32) 加粗 | — |
| 正文（目的/仪器/原理/步骤） | 宋体 | 小四(sz24) | 1.5 倍 |
| 小节标题（`实验目的：`等） | 宋体 | 小四(sz24) 加粗 | 1.5 倍 |
| 图表题注 | 宋体 | 五号(sz21) 居中 | — |
| 原始数据表 | 宋体 | 小四(sz24) 居中 | 单倍 |

这些已固化在 `labkit.py` 里，正常调用 `cell_para` / `style_run` 就不会错。
**不要手搓 OOXML**——中文字体必须同时设 `w:eastAsia`，下标必须用 `vertAlign`，
元素顺序还要符合 CT_RPr 的 schema，python-docx 已经处理好了。

---

## 已知坑（都踩过，别再踩）

1. **LibreOffice 不认表格单元格里的「与下段同页」。** 图和题注仍可能被分页拆开。
   代码里设了 `keep_with_next`（Word 里有效），但**交付前必须把 PDF 转图目视核对**。
   若被拆开，把图缩小 0.5–1cm（`width_cm`）通常就能连同题注放回同一页。
2. **原始数据记录表的大行高**（本模板是"至少 21cm"）会把表头挤到上一页、
   数据挤到下一页。`fill_data()` 会自动把它降到 15cm 自适应，并给「原始数据记录表」
   标题加 `page_break_before`，整张表落在独立一页。换模板后要重新验证这一点。
3. **别和坏掉的依赖较劲。** 系统 Python 的 matplotlib 可能因 numpy 2.x ABI 不兼容
   而无法 import。建一个专用虚拟环境/conda 环境，一次解决，比用 Pillow 手绘绕过去划算。
4. **中文字体**：`figstyle.setup_font()` 会自动挑系统里的 CJK 字体，
   实在找不到时会明确报错。Linux 上装 `fonts-noto-cjk` 即可。
5. **soffice 不能并发。** 同时跑两个转换会互相锁死；串行执行。
6. **模板里不该有红色(FF0000)文字**：模板应已清理干净。`check` 会查这一项，
   若报出来说明模板里还留着示例或批注。
7. **实验名称里的 `/` 等字符会毁掉文件名**，`labkit.safe_name()` 会清洗，
   但实验名称本身别带这些符号。

---

## 失败处理

| 现象 | 处理 |
| --- | --- |
| 任何异常，先跑 `lab.py doctor` | 它会告诉你缺依赖、模板还是 LibreOffice |
| `lab.py profile` 报缺字段 | 逐项问用户，补进 `.profile` 后重跑 |
| `build` 报「模板标题段未找到」 | 模板结构与预期不符，比对 `reference/format.md` 后调整锚点 |
| `export` 卡住/无输出 | 检查是否有别的 soffice 在跑（`pgrep -a soffice`），杀掉后重试 |
| `check` 报插图数不足 | 原理图没画或路径写错，回绘图步骤 |
| 图与题注被分页拆开 | 缩小 `width_cm`（每次减 0.5），重新 build+export |
| 用户给的数据自相矛盾 | 把验算过程摆给用户看，让用户回去核对原始记录，不要擅自改数 |

---

## 参考文件

- `reference/format.md` —— 模板结构、锚点位置、字段对照、换模板怎么改
- `reference/figures.md` —— 原理图绘制指南 + 两个可直接复用的完整范例
